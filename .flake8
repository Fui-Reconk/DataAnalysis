[flake8]
# 行长上限：项目含大量中文注释/文案，79 字符过严，放宽到 120（与最长行 104 兼容）
max-line-length = 120
# 业务约定：GUI 关键环节（读取/匹配/过滤/统计/导出）统一兜底捕获 Exception，
# 弹窗提示而不是让程序崩溃——这是产品需求（"出错时弹窗提示，程序不崩溃"），
# 属有意为之；测试文件同理。故集中豁免 BLE001，而非逐行屏蔽。
extend-ignore = BLE001
