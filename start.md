作为一名高级Python开发专家与数据分析智能体，请依据以下需求，编写一个**完整、可直接运行**的Python脚本。该脚本需要构建一个人机交互友好的桌面应用程序。

---

### 角色设定
你是一名精通 `Pandas` 数据处理、`Tkinter` GUI框架以及文件I/O操作的资深工程师。代码需具备高健壮性（异常捕获）和清晰的模块化结构。

### 核心目标
利用Python语言，自主调用标准库及第三方模块（如 `pandas`, `openpyxl`, `tkinter` 等），实现一个**可视化数据匹配与统计工具**。

---

### 详细功能需求（必须全部实现）

#### 1. GUI界面要求（方便快捷）
- 使用 `tkinter` 构建主窗口（可选用 `ttk` 美化）。
- **布局清晰**：包含文件加载区、参数配置区、状态展示区、操作按钮区。
- **交互反馈**：界面需包含进度条（`ttk.Progressbar`）或状态栏（`Label`），在执行耗时任务（如大文件匹配）时更新提示，防止界面卡死（需调用 `update_idletasks()` 或使用多线程提示）。

#### 2. 多文件输入（接受 n 个 xls/xlsx）
- 提供一个“添加文件”按钮，允许用户通过 `filedialog` 多选 `.xls` 或 `.xlsx` 文件。
- 已添加的文件以列表（`Listbox` 或 `Treeview`）形式展示在界面中。
- 支持“移除选中”文件功能。
- 读取时自动检测并统一 `sheet_name=0`（默认第一个Sheet），若读取失败需报错提示。

#### 3. 匹配逻辑（根据唯一标识项）
- 当加载完所有表格后，**自动扫描所有表格的表头（列名）**，将并集（Union）显示在一个下拉选择框（`Combobox`）中，供用户选择“唯一标识项”（即匹配的主键）。
- 用户选定标识列后，点击“执行匹配”按钮：
  - 程序以**第一个加载的表格为基准表（左表）**，将其余 n-1 个表格通过 `pandas.merge` 进行**左连接（Left Join）**。
  - 若不同表格中的标识列名称不同（但实际含义相同），需提示用户统一名称，或允许用户映射（为简化处理，可在代码中注释说明建议预处理，但脚本需自动取所有表格中**用户选择的那一列名称**进行合并，若某表无该列名则报错）。
  - 匹配时需处理空值（NaN），确保匹配键的数据类型一致（转为字符串比较）。

#### 4. 默认输出统计项（先留空——按占位符处理）
- 匹配完成后，脚本需预留一个**专门的计算模块区域**。
- **具体操作**：在代码中定义函数 `calculate_default_stats(df)`，内部仅写入 `# TODO: 在此处添加默认统计逻辑（例如：分组计数、求和、平均值等）`。
- GUI界面上对应位置显示一个标签，文字为：“默认统计项：预留（请在代码 calculate_default_stats 中自定义）”。

#### 5. 支持用户自主设置条件（动态过滤）
- 在界面上提供**条件输入框**（如 `Entry` 或 `Text` 控件），允许用户输入 `Pandas` 查询语句（例如：`"销售额 > 1000 and 地区 == '华东'"`）。
- 提供一个“应用过滤”按钮，点击后利用 `df.query()` 或 `df.eval()` 对**当前匹配完成后的合并表**进行过滤。
- 过滤出错时（如字段不存在或语法错误），弹窗（`messagebox.showerror`）提示，不崩溃。

#### 6. 最终表格导出
- 提供一个“导出结果”按钮。
- 点击后弹出保存路径选择（`filedialog.asksaveasfilename`），默认文件名为 `匹配结果_时间戳.xlsx`。
- 使用 `pandas.ExcelWriter` 写入，且需包含两个Sheet：
  - Sheet1：合并并经过用户条件过滤后的最终数据。
  - Sheet2：`calculate_default_stats` 计算出的统计结果（若统计函数返回空，则留空或写入“未定义统计”）。

---

### 技术约束与代码规范
- **导入模块**：仅限使用 `os`, `sys`, `tkinter` (含 `ttk`, `filedialog`, `messagebox`), `pandas`, `openpyxl`。
- **异常处理**：文件读取、类型转换、匹配键缺失、过滤语句解析等关键环节必须有 `try-except`。
- **代码注释**：关键逻辑处（如合并、过滤、统计）需用中文注释。
- **启动入口**：脚本需包含 `if __name__ == "__main__":` 并实例化 `Tk()` 启动主循环。

---

### 预期交付的代码结构示例（供参考）
```python
class DataMatcherApp:
    def __init__(self, root): ... 
    def add_files(self): ...
    def load_and_show_columns(self): ... # 扫描列名并更新下拉框
    def execute_merge(self): ...
    def apply_filter(self): ...
    def export_result(self): ...
    def calculate_default_stats(self, df): 
        # 【留空区域】供用户自行编写统计逻辑
        pass
```

---

### 特别注意事项
- 匹配数据量可能较大，提醒用户耐心等待，在处理循环中强制刷新UI。
- 若用户未选择标识列就点击匹配，需弹窗提醒“请先选择唯一标识项”。
- 生成的 `.exe` 或脚本需能在 Windows/macOS 通用环境下运行（使用 `os.path` 处理路径）。

请根据以上所有描述，生成完整、可落地的 Python 代码。如果涉及界面布局，请使用 `grid` 或 `pack` 合理规划，确保窗口初始大小不小于 800x600。